// A runtime package that can be told what the application is.
package app

// ID is written by the linker and has no type of its own written down, which is
// the shape the real one has.
var ID = ""

var extraArgs string

func Main() {}
