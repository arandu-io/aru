package services_test

// A test of the exported API that stayed beside the code.
//
// It declares the external test package, so it reaches nothing this package
// keeps to itself -- which is what makes its position a choice rather than a
// constraint, and the suffix the way to say so.
