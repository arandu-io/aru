package order

// Alpine within its limit: a tab index is client state, it dies on reload
// without loss, and the server never sees it.
templ Tabs() {
	<div x-data="{ tab: 0 }">
		<button @click="tab = 0">Details</button>
		<button @click="tab = 1">History</button>
	</div>
}
