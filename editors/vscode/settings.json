{
  "//": "Copied to .vscode/settings.json of every project `aru new` creates.",

  "//1": "A .kyse.go is not Go. Without this line gopls parses it, reports an error on every directive, and the editor is red on a file that is correct.",
  "files.associations": {
    "*.kyse.go": "kyse"
  },

  "//2": "Compiled views under storage/framework/views are build output. Editable sources under resources/views remain visible.",
  "search.exclude": {
    "storage/framework/views/**": true
  },

  "//3": "gofmt ignores build tags, so formatting a .kyse.go on save would break it.",
  "[kyse]": {
    "editor.formatOnSave": false,
    "editor.tabSize": 4,
    "editor.insertSpaces": true
  },

  "//4": "The rest of the project is ordinary Go.",
  "[go]": {
    "editor.formatOnSave": true
  }
}
